export type AmazonProduct = {
  id: string;
  title: string;
  affiliateUrl: string;
  memo: string;
  tags: string;
  createdAt: string;
};

export type AffiliateSettings = {
  disclosure: string;
  accountTheme: string;
};
